version: '3'
services:
  spring-boot-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: signlanguage_api
    ports:
      - "8089:8080"
    volumes:
      - ./media:/data/media/